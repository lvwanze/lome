name: lome
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # 云开发相关（已全部移除，改用 AuthService 直连云函数）
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: ^2.2.2
  google_fonts: ^6.2.1
  flutter_dotenv: ^5.1.0
  image_picker: ^0.8.7   # ← 添加这一行

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/