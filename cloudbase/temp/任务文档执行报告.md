# Lome 任务文档执行报告

> 任务文档:`C:\Users\D_Y\Desktop\任务.md`(数据存储与隔离清单)
> 依据文档:《留言板模块开发报告》(2026-09-03)、《日历云函数部署说明》(2026-08-16)
> 执行日期:2026-09-07
> 执行方式:本地源码核查 + 腾讯云 MCP 线上环境核查 + 差距修复 + 真实调用验证

---

## 一、执行结论(总览)

任务文档共 5 节,逐节核对后:**大部分条目已由两份历史报告完成**,本次新发现并修复 **3 项差距**:

| # | 差距 | 违反条目 | 修复方式 |
| --- | --- | --- | --- |
| 1 | 线上 `calendarMonthly` 数据隔离过滤被注释(联调临时改动),第三方用户数据会泄露给任意登录用户 | 任务文档 1.3 查询范围=双方可见 | 用本地已恢复隔离的代码重新部署,并移除调试日志 |
| 2 | `uploadImage` 无业务目录隔离,所有图片存 `images/<userId>/` | 任务文档 3.2 按业务隔离路径 | 增加 `folder` 白名单参数(`calendar/records`、`calendar/plans`、`messages`),已重新部署 |
| 3 | `records` / `plans` 集合缺少 `date` 字段索引 | 任务文档 1.5 索引列 | 线上已创建 `idx_date`(顺带补了 `important_days`) |

其余条目(云函数清单、字段存储、messages 索引、路由、返回格式、数据隔离规则)均已核实为**已满足**,本次未重复劳动。

---

## 二、任务文档逐节核对结果

### 2.1 一、日历模块

| 条目 | 结论 | 依据 |
| --- | --- | --- |
| 1.1 文字存储(records.content/plans.title/plans.content/records.mood) | ✅ 已满足 | 源码核查:recordCreate 写 content/mood、planCreate 写 title/content;calendarDaily 按 date 查询返回 records[]/plans[] |
| 1.2 图片存储(records.images[]/plans.images[],路径 calendar/records/、calendar/plans/) | ✅ 本次补齐 | 图片路径隔离由 uploadImage 统一实现(见差距 #2);images[] 字段本就存在 |
| 1.3 数据隔离规则 | ✅ 本次修复 | calendarMonthly 线上版过滤被注释,已恢复(见差距 #1);其余函数(record/plan CRUD、calendarDaily)隔离逻辑均在 |
| 1.4 需要检查的云函数(9 个) | ✅ 全部存在且字段正确 | 线上 30 个函数全部 Active;9 个函数代码逐一核查字段匹配 |
| 1.5 集合 records/plans + (date) 索引 | ✅ 本次补齐 | 集合已存在;`idx_date` 索引本次创建(见差距 #3) |

### 2.2 二、留言板模块

| 条目 | 结论 | 依据 |
| --- | --- | --- |
| 2.1 文字存储(content/emotionTag/replyContent) | ✅ 已满足 | messageCreate 写 content/emotionTag/replyContent(空);messageReply 覆盖式写 replyContent |
| 2.2 图片存储(messages.images[],路径 messages/) | ✅ 本次补齐 | images[] 字段存在;`messages/` 上传路径由 uploadImage folder 参数实现 |
| 2.3 数据隔离规则(我的/Ta的、仅作者改删、仅接收方回应) | ✅ 已满足 | messageList 双重条件查询;messageUpdate/Delete 仅作者;messageReply 仅接收方;messageDetail inCoupleScope 可见性;均有线上实测(见留言板开发报告 4.1/4.2) |
| 2.4 "我的留言"/"Ta的留言" 区分逻辑 | ✅ 已满足 | messageList `type=mine/partner` 游标分页,authorId 条件区分,附带 unreadCount |
| 2.5 需要检查的云函数(8 个) | ✅ 全部存在且字段正确 | 8 个 message* 函数线上 Active;代码逐一核查 content/emotionTag/images/replyContent/isRead 字段 |
| 2.6 messages 集合 + idx_author/idx_partner/idx_time_desc | ✅ 已满足 | 线上 messages 集合 5 个自定义索引(idx_author、idx_partner、idx_time_desc、idx_reply、idx_scheduled)全部存在 |

### 2.3 三、图片存储(两模块统一隔离)

| 条目 | 结论 | 说明 |
| --- | --- | --- |
| 3.1 uploadImage 函数 + 路由 `/api/v1/upload/image` + POST | ✅ 已满足 | 函数 Active,网关路由存在(2026-08-16 创建) |
| 3.2 路径规范(calendar/records、calendar/plans、messages) | ✅ 本次补齐 | folder 白名单参数,实测三个目录均落盘正确 |
| 3.3 请求参数 {file, folder} | ✅ folder 已支持 | `file` 以 `fileContent`(base64)传递而非 multipart:云开发 Event 函数经 HTTP 访问服务网关仅透传 JSON body,multipart 不可行,base64 为项目既定方案(见部署说明),属合理折中 |
| 3.4 返回格式 {code, data:{url, fileId}} | ✅ 已满足 | 实测返回完全一致 |

### 2.4 四、三模块数据隔离对比 / 五、测试数据模板

- 对比表中各项权限规则均已落实(源码核查 + 两份报告的 48 用例 / 16 用例实测)。
- 测试数据模板为参考性质,与线上集合字段一致,无执行动作。

---

## 三、本次修复详情

### 3.1 修复 1:恢复 calendarMonthly 数据隔离

**问题**:线上函数代码中 `inCoupleScope` 过滤被注释(联调临时改动),月历接口向任意登录用户返回**全库** records/plans/important_days;同时残留调试日志(打印全部重要日数据)。

**验证(真实调用)**:

| 调用方 | 结果 | 结论 |
| --- | --- | --- |
| 用户3787(4355 的伴侣)查 2026-08 | `recordedDays=0`,所有天无标记 | ✅ 用户3378(第三方)的 9 条记录不再泄露 |
| 用户4355(3378 的 partnerId)查 2026-08 | `recordedDays=9`,08-17 有 6 条记录、08-19 有 2 条规划等,与库中数据一致 | ✅ 共享数据正常可见 |

### 3.2 修复 2:uploadImage 业务路径隔离

**改动**:[uploadImage/index.js](../../functions/uploadImage/index.js) 新增 `folder` 参数,白名单校验,落盘路径 `<folder>/<userId>/<时间戳>_<随机>.<ext>`;不传 folder 时兼容旧路径 `images/<userId>/`(不影响现有 Flutter 客户端)。

**验证(真实调用,4 用例全过,测试图片已清理)**:

| 用例 | 结果 |
| --- | --- |
| `folder=messages` | ✅ `cloud://.../messages/<userId>/xxx.png` |
| `folder=calendar/records` | ✅ `cloud://.../calendar/records/<userId>/xxx.png` |
| `folder=../evil` | ✅ `{code:400, "folder 仅支持 calendar/records、calendar/plans、messages"}`(路径穿越被拦截) |
| 不传 folder | ✅ 旧路径 `images/<userId>/xxx.png`(向后兼容) |

### 3.3 修复 3:补齐 date 索引

线上创建(`idx_date`,date 升序,普通索引):

| 集合 | 索引 | 状态 |
| --- | --- | --- |
| records | idx_date | ✅ 已创建,已被实际查询命中(Ops=2) |
| plans | idx_date | ✅ 已创建,已被实际查询命中(Ops=2) |
| important_days | idx_date | ✅ 已创建(部署说明建议项,顺带补齐) |

---

## 四、线上环境最终状态

- **云函数**:30 个全部 Active(Nodejs18.15,Event 类型);本次重新部署 `uploadImage`、`calendarMonthly` 2 个。
- **集合**:records(9 条)、plans(3 条)、messages(空,测试数据已清)、important_days、users(19 条)等 8 个。
- **索引**:messages 5 个自定义索引;records/plans/important_days 各 1 个 `idx_date`。
- **网关路由**:`/api/v1/*` 全部 21 条路由就绪(calendar/record/plan/important/messages/upload)。
- **存储**:测试图片已全部清理,无残留。

---

## 五、遗留事项与建议

1. **客户端配合**:留言板发布图片时,Flutter 端应调用 uploadImage 时传 `folder: "messages"`(日历侧传 `calendar/records` / `calendar/plans`),否则落回旧目录(功能不受影响,仅目录未按业务隔离)。
2. **用户3378 等单边绑定残留**:用户3378/3287/9714/2248 的 partnerId 指向 4355,但 4355 的伴侣是 3787。这是历史测试数据,`inCoupleScope`(authorId||partnerId 双向)下 4355 仍能看见这些数据,建议确认后清理。
3. **messages 集合当前为空**:留言板联调开始后数据量增长,建议关注 idx_time_desc 的游标分页表现。
4. 本报告未重复执行的条目(8 个留言函数、13 个日历函数的完整功能测试)见《留言板模块开发报告》四/五节与《日历云函数部署说明》第 5 节。
